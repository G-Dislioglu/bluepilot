# Review Packet - BP-002

TASK_ID: BP-002
TASK_NAME: Harden Task Lock Verifier Metadata Checks
TASK_TYPE: code_task
MODE: standard
COMPLETED_AT: 2026-05-28

---

## 1. GOAL

Harden the WLP verifier so invalid task metadata and evidence contracts fail before downstream tasks rely on them.

---

## 2. PREFLIGHT CHECK

`node tools/verify-task-lock.cjs BP-002 --preflight`

Result: PASS

`git status --short` before task:

```text
(clean)
```

---

## 3. CHANGED_FILES

Expected final changed files:

```text
contracts/BP-002.json
tools/verify-task-lock.cjs
review-packets/BP-002.md
```

---

## 4. DOD_CHECK

- [x] `node --check tools/verify-task-lock.cjs` exits 0.
- [x] `node tools/verify-task-lock.cjs BP-002 --verify` exits 0.
- [x] Invalid contracts for bad `task_type`, missing `reuse_target`, bad `ui_task` `target_persona`, and missing required evidence fail with exit 1.
- [x] `git diff --check` exits 0.

---

## 5. CLAIMS_CHECK

- [x] The verifier validates `task_type` against the WLP task type set. Proven by invalid-contract test `bad-task-type: exit 1`.
- [x] The verifier validates `evidence_required` against task type requirements. Proven by invalid-contract test `missing-required-evidence: exit 1`.
- [x] The verifier validates `reuse_target` and `ui_task` `target_persona`. Proven by invalid-contract tests `missing-reuse-target: exit 1` and `bad-ui-target-persona: exit 1`.
- [x] The verifier remains compatible with the BP-002 contract under active task changes. Proven by `node tools/verify-task-lock.cjs BP-002 --verify`.

---

## 6. ASSUMPTIONS_CHECK

- [x] BP-002 does not require package.json or dependency changes. Validated: no package file changed.
- [x] BP-002 should not implement the Phase Scanner runtime. Validated: `tools/phase-scanner.cjs` not created.
- [x] The WLP evidence matrix in `docs/WORKCELL_LOCK_PROTOCOL.md` is the source for required evidence groups. Validated: verifier rules mirror the documented task type matrix.

---

## 7. EVIDENCE

| Typ | Befehl / Referenz | Ergebnis |
|---|---|---|
| test_result | `node --check tools/verify-task-lock.cjs` | PASS: exit 0 |
| test_result | invalid-contract harness in `%TEMP%` | PASS: four invalid contracts exit 1 |
| drift_check | `node tools/verify-task-lock.cjs BP-002 --verify` | PASS: no drift |
| content_check | `git diff --check` | PASS: exit 0 |

Invalid-contract results:

```text
bad-task-type: exit 1
missing-reuse-target: exit 1
bad-ui-target-persona: exit 1
missing-required-evidence: exit 1
```

---

## 8. DRIFT_CHECK

`node tools/verify-task-lock.cjs BP-002 --verify`

```text
VERIFY - BP-002
Mode: standard  Type: code_task  Risk: medium

Geaenderte Dateien:
  contracts/BP-002.json
  tools/verify-task-lock.cjs
  review-packets/BP-002.md

FORBIDDEN_FILES: Keine Verletzungen
ALLOWED_FILES: Alle Aenderungen im Scope
KEIN DRIFT - Task vollstaendig im Lock
```

Forbidden violations: NEIN
Outside ALLOWED: NEIN

---

## 9. AUDITOR_FINDINGS

- `BP-001 --verify` cannot pass while BP-002 changes are uncommitted because BP-001 forbids `tools/**`; BP-002's DoD was corrected to avoid that false requirement.
- The fallback matcher is intentionally minimal and exists only as a no-install safety path; normal installed `picomatch` remains preferred.
- Evidence validation checks declared evidence types, not whether a review packet later contains truthful evidence. That remains a review/audit responsibility.

---

## 10. SELF_SCORE

Score: 88

Begruendung:

- DoD erfuellt: 4/4.
- Claims bewiesen: 4/4.
- Assumptions validiert: 3/3.
- Drift: none.
- Evidence: strong for verifier metadata hardening.

---

## 11. DECISION

- [x] COMPLETE  Score >= 75, kein Drift, alle Pflicht-Evidence gruen
- [ ] REWORK    Score 50-74 oder minor Drift
- [ ] STOP      Score < 50 oder Forbidden-Violation

Begruendung: BP-002 is complete as a verifier hardening task: required contract metadata is now validated and positive/negative checks pass.

Self-Score ist Signal. Finales Gate ist Gurcan oder VAL-K2.

---

## 12. REUSE_NOTE

Wichtig fuer BP-003: new contracts must declare valid `task_type`, `reuse_target`, and evidence that matches WLP task type rules. The Phase Scanner MVP can rely on the verifier to reject malformed task metadata, but it still needs its own scanner input/output validation.

SESSION-LOG-Eintrag:

- Ergebnis: COMPLETE
- Score: 88
- Drift: keine
- Key Finding: WLP verifier now rejects malformed task metadata and evidence declarations before drift checks.
- Reuse durch: BP-003
