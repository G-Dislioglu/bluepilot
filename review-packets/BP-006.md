# Review Packet - BP-006

TASK_ID: BP-006
TASK_NAME: Add Plain Summary Output Rule
TASK_TYPE: governance_task
MODE: lite
COMPLETED_AT: 2026-05-28

---

## 1. GOAL

Add a simple rule that Codex final outputs must briefly explain what was done and what should happen next.

---

## 2. PREFLIGHT CHECK

`node tools/verify-task-lock.cjs BP-006 --preflight`

Result: PASS

`git status --short` before task:

```text
(clean)
```

---

## 3. CHANGED_FILES

Expected final changed files:

```text
contracts/BP-006.json
AGENTS.md
review-packets/BP-006.md
```

---

## 4. DOD_CHECK

- [x] `AGENTS.md` contains the new output rule.
- [x] `node tools/verify-task-lock.cjs BP-006 --verify` exits 0.
- [x] `git diff --check` exits 0.

---

## 5. CLAIMS_CHECK

- [x] `AGENTS.md` now requires short, plain-language final summaries. Proven by section `Ausgabe-Regel`.
- [x] `AGENTS.md` now requires a short next-step recommendation in final outputs. Proven by section `Ausgabe-Regel`.

---

## 6. ASSUMPTIONS_CHECK

- [x] This is a communication governance rule, not a product behavior change. Validated: only `AGENTS.md` changed.
- [x] No tool or runtime change is needed. Validated: no tool/runtime files changed.

---

## 7. EVIDENCE

| Typ | Befehl / Referenz | Ergebnis |
|---|---|---|
| diff_ref | `node tools/verify-task-lock.cjs BP-006 --verify` | PASS: no drift |
| content_check | `AGENTS.md` section `Ausgabe-Regel` | PASS |
| content_check | `git diff --check` | PASS |

---

## 8. DRIFT_CHECK

`node tools/verify-task-lock.cjs BP-006 --verify`

```text
FORBIDDEN_FILES: Keine Verletzungen
ALLOWED_FILES: Alle Aenderungen im Scope
KEIN DRIFT - Task vollstaendig im Lock
```

Forbidden violations: NEIN
Outside ALLOWED: NEIN

---

## 9. AUDITOR_FINDINGS

- The new rule is intentionally short; it changes communication style, not build behavior.
- The rule is placed near role/autonomy guidance so future agents see it before task rules.

---

## 10. SELF_SCORE

Score: 95

Begruendung:

- DoD erfuellt: 3/3.
- Claims bewiesen: 2/2.
- Assumptions validiert: 2/2.
- Drift: none.
- Evidence: sufficient for governance rule.

---

## 11. DECISION

- [x] COMPLETE  Score >= 75, kein Drift, alle Pflicht-Evidence gruen
- [ ] REWORK    Score 50-74 oder minor Drift
- [ ] STOP      Score < 50 oder Forbidden-Violation

Begruendung: BP-006 is complete: the requested output rule is now part of `AGENTS.md`.

---

## 12. REUSE_NOTE

Wichtig fuer den naechsten Task: final answers should briefly state what was done and what the next sensible step is, in plain language.

SESSION-LOG-Eintrag:

- Ergebnis: COMPLETE
- Score: 95
- Drift: keine
- Key Finding: Final outputs now require a short plain-language summary plus next-step recommendation.
- Reuse durch: next task
