# BPK-232 Review Packet - Runtime Patch Permit-Consume Dependency Map

## Scope

BPK-232 is a read-only decision map for the runtime patch permit-consume tower.

It does not delete files and does not change source, tests, routes, env, DB, deploy,
provider, write, runtime, activation, package, or workflow behavior.

## Findings

Measured against `origin/main` / `58752e5`:

| Check | Result |
| --- | --- |
| Cluster source files | 41 |
| Cluster source lines | 5961 |
| Direct matching tests | 41 |
| Direct matching test lines | 3351 |
| External source importers | 0 |
| External test importers | 0 |
| `builder/src/server.ts` route mounts for cluster | 0 |
| `builder/data/bpk-governance-manifest.json` cluster refs | 0 |

The cluster is separate from `builder/src/permitApply.ts`. `permitApply` is retained
and already protected by BPK-230 and BPK-231.

## Wiring Answer

This block is wired into the build path as:

- `contracts/BPK-232.json` for task-lock scope.
- `docs/RUNTIME-PATCH-PERMIT-CONSUME-DEPENDENCY-MAP.md` for the durable decision map.
- `STATE.md` and `docs/SESSION-LOG.md` for the next-step anchor.

It is not merely a chat note. Future Builder work can see the map from the repo.

## Recommendation

BPK-233 can be the first actual trim task if the human operator approves deletion.

Recommended BPK-233 shape:

- Delete only the 41 cluster source files and their 41 direct tests.
- Do not touch retained safety/write/provider/runtime surfaces.
- Run typecheck, full tests, task-lock verify, and diff-check.
- Stop if typecheck or tests reveal any hidden dependency.

## Evidence

| Command | Result |
| --- | --- |
| Dependency scan | PASS - 41 source files, 41 tests, 0 external source importers, 0 external test importers |
| `builder/src/server.ts` scan | PASS - 0 cluster route mounts |
| `builder/data/bpk-governance-manifest.json` scan | PASS - 0 cluster refs |

## Reviewer Focus

Confirm that BPK-232 is only a map. The deletion decision is intentionally left for a
separate BPK-233 human-approved task.
