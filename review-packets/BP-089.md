# Review Packet - BP-089

Datum: 2026-05-28
Task: BP-089 - BP-C1 Council Watch Loop And Init

## Ziel

BP-089 macht den BP-C1 Council Kernel lokal event-getrieben: Session-Init plus Watch-Loop fuer Agent-Dateien.

## Kritische Pruefung des externen Vorschlags

Claude schlug `chokidar.watch(agentsDir)` vor.

Anpassung durch Codex:

- Kein `chokidar`, weil das eine neue Package-/Lockfile-Frage erzeugen wuerde.
- Stattdessen dependency-freier `fs.watch`-Loop mit Debounce.
- Keine BP-C2-Module, kein Agent-Spawning, keine UI.

## Was wurde geaendert?

- `tools/maya-council-watcher.cjs` um `createSession(...)` und `watchAgents(...)` erweitert.
- CLI erweitert um `init` und `watch`.
- `tools/test-council-kernel-fixtures.cjs` erweitert um Init-, Filter- und Watch-Loop-Test.
- `docs/COUNCIL_KERNEL_RUNTIME_CONTRACT_v0.md` um BP-089-Regeln ergaenzt.
- `contracts/BP-089.json` angelegt.

## Sicherheitsgrenzen

BP-089 macht nicht:

- keine neue Dependency,
- keine Package- oder Lockfile-Aenderung,
- keine BP-C2-Module,
- kein echtes Agent-Spawning,
- keine UI,
- keine Auth,
- keine Secrets,
- keine DB,
- kein Deploy,
- kein Live Builder,
- kein AICOS-Write,
- kein Soulmatch-Copy,
- kein committed `.bluepilot/` Runtime-State.

## Evidence

Auszufuehren:

```bash
node --check tools/maya-council-watcher.cjs
node tools/test-council-kernel-fixtures.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-089 --verify
git diff --check
git diff --name-only
```

## Auditor Findings

- Watch-Loop ignoriert Nicht-JSON- und Temp-Dateien.
- Watch-Loop verarbeitet geaenderte Agent-Dateien per Debounce.
- Session-Init schreibt `session.json`, `dedup.json` und `session_opened`.
- Existing Runtime-State wird ohne `force` nicht ueberschrieben.
- Tests laufen in Temp-Verzeichnissen.

Findings: keine blockierenden Findings.

## Verification Result

Ausgefuehrt:

```bash
node --check tools/maya-council-watcher.cjs
node --check tools/test-council-kernel-fixtures.cjs
node tools/test-council-kernel-fixtures.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-089 --verify
git diff --check
```

Ergebnis: gruen.

## Decision

Entscheidung: COMPLETE

## Reuse Note

Der naechste BP-C1-Folgetask sollte eine CLI-freundliche Demo-/Smoke-Session definieren oder BP-C1 als "kernel ready" gegen Canvas-Regeln bewerten. BP-C2 bleibt blockiert, bis dieser Abschluss explizit dokumentiert ist.
