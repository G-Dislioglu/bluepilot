# Review Packet - BP-097

Datum: 2026-05-28
Task: BP-097 - BP-C3 AICOS Auto-query Read-only Slice

## Ziel

BP-097 baut AICOS Auto-query als read-only Keyword-Match gegen AICOS-Indexdaten.

## Kritische Pruefung des externen Vorschlags

Claude schlug Keyword-Match gegen lokale Snapshots oder Raw-GitHub-Index vor.

Anpassung durch Codex:

- Tests laufen offline mit lokaler Fixture.
- Remote-Fetch ist optional und read-only.
- Keine Embeddings, keine Vector DB, kein AICOS-Write.
- Output liefert Card IDs, Scores und Gruende.

## Was wurde geaendert?

- `tools/aicos-query.cjs` angelegt.
- `tools/test-aicos-query-fixtures.cjs` angelegt.
- `docs/BP_C3_AICOS_AUTO_QUERY_CONTRACT_v0.md` angelegt.
- Review-Suite erweitert.
- `contracts/BP-097.json` angelegt.

## Evidence

Auszufuehren:

```bash
node --check tools/aicos-query.cjs
node tools/test-aicos-query-fixtures.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-097 --verify
git diff --check
```

## Auditor Findings

- AICOS bleibt read-only.
- Keine Registry-Datei wird geschrieben.
- Keyword-Match liefert nachvollziehbare `reasons`.
- Tests brauchen kein Netzwerk.

Findings: keine blockierenden Findings.

## Verification Result

Ausgefuehrt:

```bash
node --check tools/aicos-query.cjs
node tools/test-aicos-query-fixtures.cjs
node tools/verify-task-lock.cjs BP-097 --verify
git diff --check
```

Ergebnis: gruen.

Auditor-Korrektur:

- Phasenmarker wie `bp-c3` werden aus Query-Tokens herausgefiltert, damit sie keine falsche Relevanz erzeugen.

## Decision

Entscheidung: COMPLETE

## Reuse Note

BP-C3-Folgetasks koennen `tools/aicos-query.cjs` nutzen, um Card-Kandidaten in Contracts vorzuschlagen. Mutation bleibt verboten.
