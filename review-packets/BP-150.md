# BP-150 Review Packet - Retire Permitless Sandbox Write

## Summary

BP-150 retires `POST /probe/sandbox-write` as a permitless sandbox write side door.

- The route now returns HTTP 410 for any method, body, or environment state.
- The previous GitHub content write/delete body was removed from `builder/src/sandboxWrite.ts`.
- The proven SmartPush one-shot permit path is unchanged.

## Critical Review Of Input

Claude's corrected brief was directionally right: a 410-only early return would leave a dormant
permitless writer in the file. Local verification added two refinements:

- Bluepilot does not call `consumeWritePermit` directly; it calls Maya-core's corridor through
  `mayaBuilderGateClient`, so the claim must be framed as "no permitless sandbox-write route"
  rather than "no consume-less write in the whole system."
- `STATE.md` records BP-149, but this clone has no `contracts/BP-149.json` or
  `review-packets/BP-149.md`. BP-150 does not repair that historical drift; it records it.

## Caller Scan

`rg` found `/probe/sandbox-write` references in:

- `builder/src/server.ts`
- `builder/src/sandboxWrite.ts`
- `builder/tests/sandboxWrite.test.ts`
- `builder/src/sandboxRealWrite.ts` replacement text
- docs, contracts, and review packets

No non-test automated caller that requires a replacement write path was found.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BP-150.json` | WLP contract. |
| `builder/src/sandboxWrite.ts` | Replace permitless writer with a small HTTP 410 stub. |
| `builder/tests/sandboxWrite.test.ts` | Prove the route is retired and does not parse/echo malformed body content. |
| `STATE.md` | Record BP-150 state and next-step guidance. |
| `docs/SESSION-LOG.md` | Record BP-150 session entry. |
| `review-packets/BP-150.md` | This packet. |

## Safety Invariants

- `BLUEPILOT_SANDBOX_PERMIT_WRITE_ENABLED` no longer enables a content write in `builder/src`.
- `builder/src/sandboxWrite.ts` no longer imports or calls GitHub write helpers.
- `builder/src/opusSmartPush.ts` is untouched.
- No live GitHub write was performed.

## Evidence

| Check | Result |
| --- | --- |
| Preflight | PASS |
| Caller scan | PASS - no non-test automated caller found |
| `npm test` in `builder/` | PASS - 47/47 |
| `npm run typecheck` in `builder/` | PASS |
| `rg -n "BLUEPILOT_SANDBOX_PERMIT_WRITE_ENABLED" builder/src` | PASS - no matches |
| `rg -n "putFileContent|outboundFetch|deleteSandboxFile|GITHUB_TOKEN|GITHUB_PAT|GH_TOKEN" builder/src/sandboxWrite.ts` | PASS - no matches |
| `node tools/verify-task-lock.cjs BP-150 --verify` | PASS - KEIN DRIFT |
| `git diff --check` | PASS |

## Deploy Note

After merge and deploy, `POST /probe/sandbox-write` and `GET /probe/sandbox-write` should both
return HTTP 410 `sandbox_write_retired`. Future governed sandbox writes should be a separate
policy/one-shot-permit block using the proven SmartPush permit path.
