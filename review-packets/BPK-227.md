# BPK-227 Review Packet - Governance Manifest LF Checkout Stability

## Scope

BPK-227 makes `builder/data/bpk-governance-manifest.json` use LF checkout through `.gitattributes`, matching the existing `builder/data/builder-repo-index.json` convention.

## Safety Notes

- No manifest semantics were changed.
- No generator code, tests, runtime routes, provider paths, write paths, package files, secrets, environment variables, deploy configuration, or workflows were changed.
- The strict byte-for-byte manifest check stays strict; the Windows working-copy line-ending drift is removed.

## Evidence

| Command | Result |
| --- | --- |
| `npx tsx --test tests/bpkGovernanceManifest.test.ts` | PASS - 2/2 |
| `npm run typecheck` | PASS |
| `node tools/verify-task-lock.cjs BPK-227 --verify` | PASS - no drift |
| `git diff --check --cached` | PASS |
| `npm test` | PASS - 276/276 |

## Reviewer Focus

Confirm that the change is limited to LF checkout stability for the generated governance manifest and does not weaken the generator check or open any runtime, provider, write, DB, auth, deploy, or environment path.
