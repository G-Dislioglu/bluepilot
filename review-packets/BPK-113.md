# BPK-113 Review Packet - Runtime Patch Permit Issue Authority

## Summary

BPK-113 adds a side-effect-free permit issue authority builder for ready runtime patch permit preflights.

- `builder/src/runtimePatchPermitIssueAuthority.ts` exports `issueRuntimePatchPermitAuthority`.
- It requires permit, issuer, and expiry refs before a runtime patch permit artifact can be issued.
- Permit consumption, patch application, server mutation, route mutation, and execution remain false.

## Commands

| Check | Result |
| --- | --- |
| `npx tsx --test tests/runtimePatchPermitIssueAuthority.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `node tools/verify-task-lock.cjs BPK-113 --verify` | PASS - no drift |
| `git diff --check` | PASS |
| `npm test` in `builder/` | PASS - 173/173 tests |

## Scope Notes

- Runtime patch permit issue authority only; no permit consumption, patch apply, server mount, route mutation, runtime execution, DB, package, workflow, deploy, durable write, or external action.
