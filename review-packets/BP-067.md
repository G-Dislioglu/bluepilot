# Review Packet - BP-067

Datum: 2026-05-28
Task: BP-067 - Define Builder Task Create Readiness Contract

## Ziel

BP-067 definiert den naechsten Handoff:

```text
Auth/Persistence Readiness Candidate -> Builder Task Create Readiness Candidate
```

Der Block baut keinen Builder Task Create, keinen Live Builder Call und keine Execute-Logik.

## Was wurde geaendert?

- `docs/BUILDER_TASK_CREATE_READINESS_CONTRACT_v0.md` angelegt.
- `contracts/BP-067.json` angelegt.

## Sicherheitsgrenzen

BP-067 macht nicht:

- kein Builder Task Create,
- kein Live Builder Call,
- keine Builder Adapter Konfiguration,
- keine Auth,
- keine Secrets,
- keine DB oder Persistenz,
- keine echte Approval-Wirkung,
- kein Approval Recording,
- kein Execute / Approve / Push / Deploy,
- keine Ziel-Datei-Writes,
- keine UI.

## Ergebnis

Der Handoff legt fest:

- welche Auth/Persistence-Readiness-Felder fuer Builder Task Create Readiness Pflicht sind,
- warum `builder_adapter_mode`, `task_create_effect_requested` und `execute_effect_requested` im MVP `none` bleiben muessen,
- warum `builder_task_create_allowed`, `builder_execute_allowed` und `live_builder_call_allowed` weiterhin `false` bleiben muessen,
- welche Intent-Werte erlaubt und verboten sind,
- wann blockiert wird,
- wann Human Review noetig bleibt.

## Evidence

Auszufuehren:

```bash
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-067 --verify
git diff --check
```

## Review-Fragen

1. Ist klar, dass BP-067 noch keinen Builder Task Create baut?
2. Ist klar, dass BP-067 keinen Live Builder Call erlaubt?
3. Bleiben Auth, Secrets und Persistenz unimplementiert?
4. Bleibt Execute sichtbar blockiert?
5. Ist BP-068 als lokaler Mock der sichere naechste Schritt?

## Naechster Schritt

BP-068 sollte einen lokalen Builder-Task-Create-Readiness-Candidate Mock mit Fixtures bauen.
