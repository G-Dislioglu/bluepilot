# Review Packet - BP-046

Datum: 2026-05-28
Task: BP-046 - Add Scope Resolver Review Suite Coverage

## Ziel

BP-046 haengt den lokalen Scope Resolver aus BP-045 in die gemeinsame Bluepilot Review Suite ein.

Ausserdem wird ein Invariant-Test ergaenzt, der die wichtigsten Sicherheitsflags schuetzt.

## Was wurde geaendert?

- `tools/test-scope-resolver-output-invariants.cjs` angelegt.
- `tools/test-bluepilot-review-suite.cjs` um Scope Resolver Checks erweitert.
- `docs/BLUEPILOT_REVIEW_SUITE_v0.md` aktualisiert.
- `contracts/BP-046.json` angelegt.

## Sicherheitsgrenzen

BP-046 aendert nicht:

- Scope Resolver Runtime,
- Scope Resolver Fixtures,
- Builder Adapter,
- Phase Scanner Runtime,
- Package-Metadaten,
- Auth / Secrets / DB / Deploy.

## Invariant-Schutz

Der neue Test prueft fuer alle Scope Resolver Input-Fixtures:

- Status ist einer von `resolved`, `requires_human_review`, `blocked`,
- `requires_human_gate` bleibt `true`,
- `writes_allowed_now` bleibt `false`,
- `task_create_allowed` bleibt `false`,
- Output-Listen bleiben strukturell stabil,
- `blocked` enthaelt Blockgruende,
- `requires_human_review` erlaubt keine Write-Kandidaten.

## Evidence

Auszufuehren:

```bash
node --check tools/test-scope-resolver-output-invariants.cjs
node tools/test-scope-resolver-output-invariants.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-046 --verify
git diff --check
```

## Review-Fragen

1. Ist die Review Suite jetzt der richtige Einstieg fuer Scope Resolver Checks?
2. Bleibt Task Create trotz Scope Resolver weiterhin sichtbar blockiert?
3. Sind die Output-Invarianten eng genug fuer den naechsten Builder-Handoff?
4. Wurde keine Runtime-Logik still veraendert?

## Naechster Schritt

BP-047 sollte eine Scope Resolver Coverage Map ergaenzen oder den naechsten Handoff Richtung Builder Task Contract rein dokumentarisch abgrenzen.
