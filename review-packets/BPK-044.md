# BPK-044 Review Packet - Live AICOS Memory Cache Store Shell

## Summary

BPK-044 adds an in-process store shell for explicit Live AICOS memory cache entries.

- `builder/src/liveAicosMemoryCacheStoreShell.ts` exports `createLiveAicosMemoryCacheStoreShell`.
- It supports put/read/invalidate/snapshot in process memory only.
- Reads reuse the existing fresh/stale/missing semantics.
- Durable persistence remains closed.

## Commands

| Check | Result |
| --- | --- |
| `npx tsx --test tests/liveAicosMemoryCacheStoreShell.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `node tools/verify-task-lock.cjs BPK-044 --verify` | PASS - no drift |
| `git diff --check` | PASS |
| `npm test` in `builder/` | PASS - 105/105 tests |

## Scope Notes

- No durable store, DB, filesystem cache, scheduler, route, external call, deploy, workflow, or package change.
