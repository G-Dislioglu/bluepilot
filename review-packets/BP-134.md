# BP-134 Review Packet - Scrub BP-131 Identifiers

## Scope

BP-134 removes concrete DB resource identifiers that remained in the old BP-131 contract and
review packet. It does not change Builder code, tests, packages, root package files, tools, or
soulmatch.

## Files Changed

| File | Reason |
|---|---|
| `contracts/BP-134.json` | Task contract, without repeating concrete identifiers |
| `contracts/BP-131.json` | Replaces concrete project/branch values with placeholders |
| `review-packets/BP-131.md` | Replaces concrete project/branch/endpoint/old-project values with placeholders |
| `docs/SESSION-LOG.md` | Anker log entry |
| `STATE.md` | Current state and next-step hints |
| `review-packets/BP-134.md` | Evidence packet |

## Scrubbed Values

Concrete resource values were replaced with neutral placeholders:

| Location | Replacement |
|---|---|
| BP-131 Neon project value | `<neon-project-id>` |
| BP-131 Neon branch value | `<neon-branch-id>` |
| BP-131 Neon endpoint value | `<neon-endpoint>` |
| Old soulmatch project value | `<soulmatch-project-id>` |

The human service label `bluepilot-builder` stays. The default values `neondb` and `neondb_owner`
also stay because they are Neon product defaults, not coordinates that identify this project.

## Verification

The current working tree was scanned for the four known concrete identifier strings. Result:

```text
no known BP-131 identifiers found in tracked working tree
```

`contracts/BP-131.json` was parsed as JSON after the scrub:

```text
BP-131 json ok
```

## Boundary

Git history is unchanged by design. A full history scrub would require a separate explicit
decision, such as a history rewrite or making the repository private.

## Commands

```text
node tools/verify-task-lock.cjs BP-134 --verify
git diff --check
```

## Result

BP-134 makes the current tracked working tree identifier-free for the known BP-131 concrete
resource values while preserving the readability of the old BP-131 evidence.
