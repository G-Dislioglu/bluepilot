# Review Packet - BP-088

Datum: 2026-05-28
Task: BP-088 - BP-C1 Council Kernel Minimal Runtime

## Ziel

BP-088 baut den ersten lokalen BP-C1 Council-Kernel-Slice: Agent Client plus Maya Watcher als deterministische Node-CJS-Tools.

## Kritische Pruefung des externen Vorschlags

Claude schlug `maya-council-watcher.ts` und `council-agent-client.ts` als erste Runtime vor.

Anpassung durch Codex:

- `.ts` nicht blind uebernommen, weil das aktuelle Bluepilot-Repo lokale Tools als `.cjs` fuehrt.
- Runtime klein gehalten: kein BP-C2, kein Agent-Spawning, kein UI.
- Council Session Protocol v1.1 als verbindliche Quelle genutzt.
- Keine `.bluepilot/` Runtime-Dateien committed; Tests nutzen Temp-Verzeichnisse.

## Was wurde geaendert?

- `docs/COUNCIL_KERNEL_RUNTIME_CONTRACT_v0.md` angelegt.
- `tools/council-agent-client.cjs` angelegt.
- `tools/maya-council-watcher.cjs` angelegt.
- `tools/test-council-kernel-fixtures.cjs` angelegt.
- `tools/test-bluepilot-review-suite.cjs` erweitert.
- `contracts/BP-088.json` angelegt.

## Sicherheitsgrenzen

BP-088 macht nicht:

- keine BP-C2-Module,
- kein echtes Agent-Spawning,
- keine UI,
- keine Auth,
- keine Secrets,
- keine DB,
- kein Deploy,
- kein Live Builder,
- kein AICOS-Write,
- kein Soulmatch-Copy,
- kein committed `.bluepilot/` Runtime-State.

## Evidence

Auszufuehren:

```bash
node --check tools/council-agent-client.cjs
node --check tools/maya-council-watcher.cjs
node tools/test-council-kernel-fixtures.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-088 --verify
git diff --check
git diff --name-only
```

## Auditor Findings

- Agent Client schreibt nur Agent-Dateien und Events, nicht `session.json`.
- Directive-Ack erfolgt ueber `directive_cursor`.
- Watcher schreibt `session.json` und `dedup.json`.
- HARD STOP setzt Session auf `paused` und erzeugt Broadcast.
- Dedup-Key nutzt `session_id`, `agent_id`, `task_id`, `task_attempt`, `status`, `status_revision`.
- Tests pruefen Done-Flow, Dedup und HARD STOP.

Findings: keine blockierenden Findings.

## Verification Result

Ausgefuehrt:

```bash
node --check tools/council-agent-client.cjs
node --check tools/maya-council-watcher.cjs
node --check tools/test-council-kernel-fixtures.cjs
node tools/test-council-kernel-fixtures.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-088 --verify
git diff --check
```

Ergebnis: gruen.

## Decision

Entscheidung: COMPLETE

## Reuse Note

Der naechste BP-C1-Folgetask sollte den Watcher um einen echten Watch-Loop mit await-write-finish und eine kleine CLI-Init-Sequenz fuer `session.json` erweitern. BP-C2 bleibt blockiert, bis BP-C1 als Council Kernel vollstaendig gruen ist.
