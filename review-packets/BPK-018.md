# BPK-018 Review Packet - Cockpit Read-Only UI

## Summary

BPK-018 adds a read-only cockpit HTML renderer.

- `builder/src/cockpitReadOnlyHtml.ts` renders a full static HTML document from a BPK-009 cockpit
  projection model.
- The renderer escapes model text and keeps all actions disabled.
- Ready, review, and blocked states are represented with read-only panels.
- No route, server wiring, runtime call, provider call, database write, GitHub write, package
  change, deployment change, or live AICOS call was added.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-018.json` | WLP contract for read-only cockpit HTML. |
| `builder/src/cockpitReadOnlyHtml.ts` | Pure static HTML renderer. |
| `builder/tests/cockpitReadOnlyHtml.test.ts` | Focused renderer tests. |
| `docs/CLAUDE-CONTEXT.md` | Records BPK-018 and next-block posture. |
| `docs/SESSION-LOG.md` | Records BPK-018 result and test evidence. |
| `STATE.md` | Records current BPK contract and next sequence. |
| `review-packets/BPK-018.md` | This review packet. |

## Safety Invariants

| Invariant | Status | Evidence |
| --- | --- | --- |
| Renderer is side-effect-free | PASS | It imports only BPK-009 types and returns HTML. |
| No route mounted | PASS | `server.ts` and route files were not changed. |
| Actions remain disabled | PASS | Tests cover disabled action markup. |
| Text is escaped | PASS | Tests cover script-like input escaping. |
| Generated preview not committed | PASS | Visual preview is temporary and outside tracked files. |

## Commands

```text
npx tsx --test tests/cockpitReadOnlyHtml.test.ts
npm run typecheck
node tools/verify-task-lock.cjs BPK-018 --verify
git diff --check
```

Observed before final verification:

| Check | Result |
| --- | --- |
| `npx tsx --test tests/cockpitReadOnlyHtml.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| Static preview visual check | PASS, Playwright screenshot rendered a nonblank read-only cockpit preview with disabled actions |

## Scope Notes

- This block renders static HTML only.
- The renderer can be mounted later by a route or served by a future UI shell.
- No generated preview artifacts are committed.

## Next Block

Live AICOS Connector through Intake may open only after BPK-018 verify, commit, and push are
complete.
