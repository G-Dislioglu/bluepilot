# BPK-001 Review Packet - Doc Drift Hygiene

## Summary

BPK-001 repairs the Bluepilot anchor drift before opening any later BPK block.

- `docs/CODEX-RICHTUNGSBRIEF-optimized.md` is now the cleaned Codex direction brief.
- `docs/CLAUDE-CONTEXT.md` now reflects the real BP-149 state from `STATE.md`.
- `docs/SESSION-LOG.md` and `STATE.md` record BPK-001.
- A real request to maya-core memory was made and recorded as `live-auth-required`.

No runtime code, builder code, package file, deploy setting, secret, DB, GitHub
write path, or maya-core code was changed.

## Critical Review Of External Claude Brief

The supplied Claude brief was directionally correct but not directly safe to
execute without correction.

Corrections applied:

- Fixed encoding drift by writing the optimized brief in ASCII-safe German.
- Resolved repo-context ambiguity by selecting the BP-149 repo candidate:
  `C:\Users\guerc\Documents\Codex\2026-06-03\files-mentioned-by-the-user-eingef-2\work\bluepilot`.
- Made `FORBIDDEN_FILES` concrete in `contracts/BPK-001.json`.
- Made `GOAL_DELTA_PROPOSAL` an explicit review-packet mechanism.
- Clarified that branch/commit/push rules depend on a real Git repo and remote.
- Clarified the maya-core memory test into factual states:
  `live`, `live-auth-required`, `not-live`, `not-checkable`.
- Resolved the autonomy conflict: later blocks do not open until BPK-001 gates
  are green.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-001.json` | WLP contract for the new BPK path. |
| `docs/CODEX-RICHTUNGSBRIEF-optimized.md` | Cleaned binding direction brief. |
| `docs/CLAUDE-CONTEXT.md` | Rebuilt BP-149 anchor truth. |
| `docs/SESSION-LOG.md` | Records BPK-001 result and next line. |
| `STATE.md` | Records BPK-001 and the BPK sequence. |
| `review-packets/BPK-001.md` | This review packet. |

## Evidence

| Check | Result |
| --- | --- |
| Repo candidate | PASS - `STATE.md` records BP-149 and HEAD was `c7c3c97` before BPK-001. |
| Bootstrap contract | PASS - `contracts/BPK-001.json` added with WLP fields. |
| Maya memory live request | PASS - `GET https://maya-core.onrender.com/api/maya/memory?origin=bluepilot` returned HTTP 401 `{"error":"unauthorized"}`. |
| Runtime/code untouched | PASS - only allowed docs/contract/review files changed. |
| UI scope | n.a. - no UI change. |

## Falsification Gates

| Gate | Status | Evidence |
| --- | --- | --- |
| G1 Repo-Wahrheit | PASS | `STATE.md` and `docs/SESSION-LOG.md` both record BP-149; `docs/CLAUDE-CONTEXT.md` now mirrors that state. |
| G2 Kein Doppelbau | PASS | Existing `tools/maya-memory*.cjs` and `builder/src/sandboxWrite.ts` were read and not duplicated. |
| G3 Scope-Treue | PASS | Scope stayed in anchor/contract/review docs only. |
| G4 FORBIDDEN_FILES | PASS | Verify checks changed files against `contracts/BPK-001.json`. |
| G5 Sicht-Test | n.a. | No UI files changed. |

## Diff Excerpt

```diff
- > `docs/SESSION-LOG.md`, dann bei Bedarf `STATE.md`. Stand: 2026-05-31.
+ > Stand: 2026-06-13. Anker aktualisiert durch BPK-001 auf reale BP-149-Wahrheit.
...
+ - BP-149: alter Permit-Demo-Trigger durch `POST /probe/sandbox-write` ersetzt.
+ - Ergebnis am 2026-06-13: HTTP 401 mit Body `{"error":"unauthorized"}`.
+ - Bewertung: `live-auth-required`.
```

## Bootstrap Note

The first BPK contract did not exist before this block. That means the normal
preflight could not be run before adding `contracts/BPK-001.json`. This is the
same practical bootstrap exception as earlier anchor work: create the contract,
then verify the full diff against that contract.

## Commands

To be run after this packet exists:

```text
node tools/verify-task-lock.cjs BPK-001 --verify
git diff --check
```

## Next Block

BPK-002 Permit-Generalisierung may open only after BPK-001 verify is green and
the commit/push state is documented.
