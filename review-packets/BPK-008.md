# BPK-008 Review Packet - Runtime Dispatch Integration Contract

## Summary

BPK-008 adds a pure runtime dispatch integration contract model.

- `builder/src/runtimeDispatchIntegrationContract.ts` classifies BPK-007 readiness projections as
  `runtime_candidate`, `operator_review`, or `blocked`.
- Dry-run eligibility is separated from write-capable runtime dispatch.
- Write-capable adoption requires an explicit authority reference.
- The output explicitly keeps executable route, provider, database, and GitHub writes closed.
- No runtime route, worker dispatch, provider call, database write, GitHub write, package change,
  deployment change, or frontend file was added.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-008.json` | WLP contract for runtime integration contract block. |
| `builder/src/runtimeDispatchIntegrationContract.ts` | Pure runtime adoption classifier. |
| `builder/tests/runtimeDispatchIntegrationContract.test.ts` | Focused classifier tests. |
| `docs/CLAUDE-CONTEXT.md` | Records BPK-008 and next-block posture. |
| `docs/SESSION-LOG.md` | Records BPK-008 result and test evidence. |
| `STATE.md` | Records current BPK contract and next sequence. |
| `review-packets/BPK-008.md` | This review packet. |

## Safety Invariants

| Invariant | Status | Evidence |
| --- | --- | --- |
| Contract is side-effect-free | PASS | It imports only the BPK-007 projection type and returns data. |
| Runtime remains closed | PASS | Orchestrator, server, provider, DB, and push files were not changed. |
| Dry-run and write are distinct | PASS | Tests cover dry-run candidate versus authority-backed write candidate. |
| Review does not become dispatch | PASS | Review readiness returns `operator_review` with dispatch disabled. |
| Missing evidence blocks adoption | PASS | Tests cover missing required evidence. |

## Commands

```text
npx tsx --test tests/runtimeDispatchIntegrationContract.test.ts
npm run typecheck
node tools/verify-task-lock.cjs BPK-008 --verify
git diff --check
```

Observed before final verification:

| Check | Result |
| --- | --- |
| `npx tsx --test tests/runtimeDispatchIntegrationContract.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |

## Scope Notes

- This is a contract and classifier, not executable runtime integration.
- `builder/src/opusTaskOrchestrator.ts` and `builder/src/server.ts` were intentionally not
  changed.
- Permit authority is represented only as an opaque evidence reference.

## Next Block

Cockpit Projection Adoption may open only after BPK-008 verify, commit, and push are complete.
