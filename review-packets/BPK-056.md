# BPK-056 Review Packet - Memory Cache Invalidation Evidence Binding

## Summary

BPK-056 binds operator invalidation evidence to the prior memory cache facade/store binding.

- `builder/src/memoryCacheInvalidationEvidenceBinding.ts` exports `bindMemoryCacheInvalidationEvidence`.
- It requires a ready prior binding, successful BPK-052 invalidation, and evidence ref.
- Durable persistence remains closed.

## Commands

| Check | Result |
| --- | --- |
| `npx tsx --test tests/memoryCacheInvalidationEvidenceBinding.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `node tools/verify-task-lock.cjs BPK-056 --verify` | PASS - no drift |
| `git diff --check` | PASS |
| `npm test` in `builder/` | PASS - 117/117 tests |

## Scope Notes

- Evidence binding only; no durable store, DB, filesystem cache, scheduler, route, external call, deploy, workflow, or package change.
