# BPK-007 Review Packet - Dispatch / Frontend zuletzt

## Summary

BPK-007 closes the requested BPK line with a pure dispatch/frontend readiness projection.

- `builder/src/dispatchFrontendReadiness.ts` combines a WLP contract draft, card-conditioned
  dispatch plan, and pre-registered claims gate.
- The projection returns `dispatch_ready`, `frontend_review`, or `blocked`.
- Dispatch is allowed only when card-conditioned dispatch and pre-registered claims both allow
  and the contract has explicit evidence requirements.
- Frontend projection remains side-effect-free and can represent blocked/review states without
  opening runtime dispatch.
- No server route, provider call, runtime dispatch, GitHub write, package change, or frontend
  file was added.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-007.json` | WLP contract for the dispatch/frontend readiness block. |
| `builder/src/dispatchFrontendReadiness.ts` | Pure readiness projection. |
| `builder/tests/dispatchFrontendReadiness.test.ts` | Focused projection tests. |
| `docs/CLAUDE-CONTEXT.md` | Records BPK-007 and next-block posture. |
| `docs/SESSION-LOG.md` | Records BPK-007 result and test evidence. |
| `STATE.md` | Records current BPK contract and next sequence. |
| `review-packets/BPK-007.md` | This review packet. |

## Safety Invariants

| Invariant | Status | Evidence |
| --- | --- | --- |
| Projection is side-effect-free | PASS | It imports only BPK types and returns a data object. |
| Runtime remains closed | PASS | Orchestrator, server, provider, push, DB, and route files were not changed. |
| Frontend remains contract-only | PASS | No UI file was added; the output is a data projection for later adoption. |
| Review is not dispatch | PASS | Review-required inputs return `frontend_review` with `dispatchAllowed:false`. |
| Missing evidence blocks dispatch | PASS | Tests cover empty `evidence_required`. |
| Task mismatch blocks dispatch | PASS | Tests cover contract/dispatch task-ID mismatch. |

## Commands

```text
npx tsx --test tests/dispatchFrontendReadiness.test.ts
npm run typecheck
node tools/verify-task-lock.cjs BPK-007 --verify
git diff --check
```

Observed before final verification:

| Check | Result |
| --- | --- |
| `npx tsx --test tests/dispatchFrontendReadiness.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `npm test` in `builder/` | PASS, 59/59 tests |

## Scope Notes

- `builder/src/opusTaskOrchestrator.ts` was intentionally not changed.
- `builder/src/server.ts` was intentionally not changed.
- `builder/src/cardConditionedDispatch.ts`, `builder/src/preRegisteredClaims.ts`, and
  `builder/src/workerPacketWlpAdapter.ts` were intentionally not changed.
- A future frontend may consume the projection, but this block deliberately adds no screen.

## Next Block

The requested BPK sequence is complete after BPK-007 verify, commit, and push. The next main
decision should be runtime adoption sequencing, not another silent pre-dispatch expansion.
