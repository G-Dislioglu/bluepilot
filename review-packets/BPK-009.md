# BPK-009 Review Packet - Cockpit Projection Adoption Contract

## Summary

BPK-009 adds a pure cockpit projection adoption contract.

- `builder/src/cockpitProjectionAdoptionContract.ts` converts BPK-007 readiness plus BPK-008
  runtime integration status into a cockpit-consumable model.
- Ready, review, blocked, and invalid states are represented deterministically.
- Blocked and review states remain renderable for operator inspection but never executable.
- No frontend file, server route, runtime call, provider call, database write, GitHub write,
  package change, or deployment change was added.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-009.json` | WLP contract for cockpit projection adoption. |
| `builder/src/cockpitProjectionAdoptionContract.ts` | Pure cockpit model adoption function. |
| `builder/tests/cockpitProjectionAdoptionContract.test.ts` | Focused adoption tests. |
| `docs/CLAUDE-CONTEXT.md` | Records BPK-009 and next-block posture. |
| `docs/SESSION-LOG.md` | Records BPK-009 result and test evidence. |
| `STATE.md` | Records current BPK contract and next sequence. |
| `review-packets/BPK-009.md` | This review packet. |

## Safety Invariants

| Invariant | Status | Evidence |
| --- | --- | --- |
| Contract is side-effect-free | PASS | It imports only BPK-007/BPK-008 types and returns data. |
| No frontend implemented | PASS | No UI, route, HTML, CSS, or React files were changed. |
| Blocked states remain inspectable | PASS | Tests cover blocked-but-renderable cockpit model. |
| Executable actions remain disabled | PASS | Output action list is always disabled in this block. |
| Task mismatch blocks adoption | PASS | Tests cover readiness/runtime task-ID mismatch. |

## Commands

```text
npx tsx --test tests/cockpitProjectionAdoptionContract.test.ts
npm run typecheck
node tools/verify-task-lock.cjs BPK-009 --verify
git diff --check
```

Observed before final verification:

| Check | Result |
| --- | --- |
| `npx tsx --test tests/cockpitProjectionAdoptionContract.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |

## Scope Notes

- This block creates a cockpit data contract only.
- UI implementation still needs a later block with visual evidence.
- Runtime action buttons are represented as disabled metadata, not behavior.

## Next Block

Live AICOS/Card Binding Intake may open only after BPK-009 verify, commit, and push are complete.
