# BP-142 Review Packet - Non-default Whole-file Write Adapter

## Scope

BP-142 fixes the final Phase-B adapter blocker: a new sandbox file is a whole-file create, but
SmartPush previously rejected overwrites/creates for non-default repos.

## Changes

| File | Change |
|---|---|
| `contracts/BP-142.json` | WLP contract, corrected from the Claude draft. |
| `builder/src/opusPatchMode.ts` | Adds `putFileContent`, a GitHub contents API create/update helper. |
| `builder/src/opusSmartPush.ts` | Routes non-default overwrites through `putFileContent`; default path remains `/push`. |
| `builder/tests/opusPatchMode.test.ts` | Proves create omits `sha` and update includes `sha`. |
| `builder/tests/opusSmartPush.test.ts` | Proves non-default routing, missing-token handling, and unchanged default `/push`. |
| `docs/SESSION-LOG.md`, `STATE.md` | Updates current state and next-step guidance. |

## Critical Safety Notes

- No endpoint, profile, orchestrator, provider, Maya gate, or deploy setting changed.
- The default `soulmatch` overwrite path still uses `/push`.
- Non-default whole-file writes require an existing GitHub token; without one, SmartPush returns a clear error.
- Tests use mocks only. No live GitHub write was performed by this task.
- Token values are not returned in results or errors.

## Verification

```text
node tools/verify-task-lock.cjs BP-142 --preflight
PASS
```

```text
npm test
PASS - 41/41 tests
```

```text
npm run typecheck
PASS
```

## Post-merge Plan

After VAL-K2, merge and deploy. Then run the BP-141 live endpoint exactly once:

```text
POST https://bluepilot-builder.onrender.com/probe/sandbox-real-write
body: {"confirm":"real-write-to-bluepilot-sandbox"}
```

Expected after write flags are staged: one real commit creating/updating
`.bluepilot/phase-b-real-write.md` in `G-Dislioglu/bluepilot-sandbox` only.
After success, turn all write env flags off and redeploy.
