# Review Packet - BP-050

Datum: 2026-05-28
Task: BP-050 - Add Builder Task Candidate Review Suite Coverage

## Ziel

BP-050 haengt den lokalen Builder Task Candidate Mock aus BP-049 in die gemeinsame Bluepilot Review Suite ein.

Ausserdem wird ein Invariant-Test ergaenzt, der die wichtigsten Sicherheitsflags schuetzt.

## Was wurde geaendert?

- `tools/test-builder-task-candidate-output-invariants.cjs` angelegt.
- `tools/test-bluepilot-review-suite.cjs` um Builder Task Candidate Checks erweitert.
- `docs/BLUEPILOT_REVIEW_SUITE_v0.md` aktualisiert.
- `contracts/BP-050.json` angelegt.

## Sicherheitsgrenzen

BP-050 aendert nicht:

- Builder Task Candidate Runtime,
- Builder Task Candidate Fixtures,
- Builder Adapter,
- Scope Resolver Runtime,
- Auth / Secrets / DB / Deploy.

## Invariant-Schutz

Der neue Test prueft fuer alle Builder Task Candidate Input-Fixtures:

- Status ist einer von `candidate_prepared`, `requires_human_review`, `blocked`,
- `human_gate_required` bleibt `true`,
- `builder_task_create_allowed` bleibt `false`,
- `builder_execute_allowed` bleibt `false`,
- `blocked` enthaelt Blockgruende,
- `requires_human_review` erlaubt keine Write-Kandidaten,
- `candidate_prepared` enthaelt eine sichtbare Notiz, dass Builder Task Create blockiert bleibt.

## Evidence

Auszufuehren:

```bash
node --check tools/test-builder-task-candidate-output-invariants.cjs
node tools/test-builder-task-candidate-output-invariants.cjs
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-050 --verify
git diff --check
```

## Review-Fragen

1. Ist die Review Suite jetzt der richtige Einstieg fuer Builder Task Candidate Checks?
2. Bleibt Task Create weiterhin sichtbar blockiert?
3. Bleibt Execute weiterhin sichtbar blockiert?
4. Wurde keine Runtime-Logik still veraendert?

## Naechster Schritt

BP-051 sollte eine Builder Task Candidate Coverage Map ergaenzen oder den naechsten Handoff Richtung Human Gate rein dokumentarisch abgrenzen.
