# Review Packet - BP-048

Datum: 2026-05-28
Task: BP-048 - Define Builder Task Contract Handoff Boundary

## Ziel

BP-048 definiert den naechsten Handoff:

```text
Scope Resolver Output -> Builder Task Contract Candidate
```

Der Block baut keinen Task Create und ruft keinen Builder auf.

## Was wurde geaendert?

- `docs/BUILDER_TASK_CONTRACT_HANDOFF_v0.md` angelegt.
- `contracts/BP-048.json` angelegt.

## Sicherheitsgrenzen

BP-048 macht nicht:

- kein Builder Task Create,
- kein Builder Execute,
- kein Live Builder Call,
- kein Approve / Push / Deploy,
- keine Auth,
- keine Secrets,
- keine DB,
- keine Persistenz,
- keine Ziel-Datei-Writes.

## Ergebnis

Der Handoff legt fest:

- welche Scope-Resolver-Felder fuer einen Task-Contract-Kandidaten Pflicht sind,
- warum `writes_allowed_now` und `task_create_allowed` weiterhin `false` sein muessen,
- welche Task Intent Werte erlaubt und verboten sind,
- wann blockiert wird,
- wann Human Review noetig ist,
- dass ein spaeterer Mock weiterhin lokal und fixture-basiert bleiben muss.

## Evidence

Auszufuehren:

```bash
node tools/test-bluepilot-review-suite.cjs
node tools/verify-task-lock.cjs BP-048 --verify
git diff --check
```

## Review-Fragen

1. Ist klar, dass BP-048 noch keinen Builder Task erstellt?
2. Bleibt der Human Gate vor Builder Task Create sichtbar?
3. Sind die harten Block-Regeln ausreichend eng?
4. Ist BP-049 als lokaler Mock der sichere naechste Schritt?

## Naechster Schritt

BP-049 sollte einen lokalen Builder-Task-Contract-Candidate Mock mit Fixtures bauen.
