# BPK-042 Review Packet - PR Receipt File Loader Contract

## Summary

BPK-042 defines a request/response contract for a future PR receipt file loader.

- `builder/src/prReceiptFileLoaderContract.ts` exports `buildPrReceiptFileLoaderContractResponse`.
- It requires POST, confirm phrase, matching path, and ready BPK-038 decision.
- Even success returns `fileReadAllowed:false`.

## Commands

| Check | Result |
| --- | --- |
| `npx tsx --test tests/prReceiptFileLoaderContract.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `node tools/verify-task-lock.cjs BPK-042 --verify` | PASS - no drift |
| `git diff --check` | PASS |
| `npm test` in `builder/` | PASS - 101/101 tests |

## Scope Notes

- No filesystem read happens in this block.
- A later contract is required for actual loader implementation.
