# BPK-229 Review Packet - Post-BPK-226 Trim Map

## Scope

BPK-229 creates a no-code trim decision map at `docs/POST-BPK-226-TRIM-MAP.md`. It sorts post-BPK-226 Bluepilot surfaces against the 10 hard caps from the existing soulmatch autonomy layer.

## Findings

- Budget/cost gating exists in the provider path: `providers.ts` calls `assertBuilderBudgetGate(...)` before provider calls and `recordBuilderCost(...)` after responses, delegating to `assessBudget` and `recordCost`.
- `permitApply.ts` is a real write endpoint and should be kept, but it needs a later `main`/`master` branch hard-cap fix.
- `activationLockBoundary.ts`, `runtimeExecutionDecision.ts`, `mayaCoreGateEnforcementContract.ts`, provider budget/cost gating, `permitApply.ts`, and `builderSafetyPolicy.ts` are useful retained surfaces.
- `activationDecisionOperatorMode.ts` and `mayaAutonomyAuthorityIntake.ts` should align their older generic hard-stop lists to the 10 soulmatch caps.
- The `runtimePatchPermitConsume...` receipt/audit tower is the strongest trim candidate, but only after a dependency graph and test impact map.

## Safety Notes

- No code was changed.
- No file was deleted.
- No runtime, provider, write, route, env, DB, deploy, package, workflow, or activation behavior was changed.
- BPK-229 is stacked on BPK-228; BPK-228 should be merged before this map becomes mainline policy.

## Evidence

| Command | Result |
| --- | --- |
| `node tools/verify-task-lock.cjs BPK-229 --preflight` | PASS |
| `rg -n "Post-BPK-226 Trim Map|10 Hard Caps|Main/master write cap|local emergency stop|runtimePatchPermitConsume|assertBuilderBudgetGate|permitApply" docs/POST-BPK-226-TRIM-MAP.md STATE.md docs/SESSION-LOG.md docs/CLAUDE-CONTEXT.md` | PASS |
| `node tools/verify-task-lock.cjs BPK-229 --verify` | PASS - no drift, 6 files in lock |
| `git diff --check --cached` | PASS |

## Reviewer Focus

Confirm that the map is only a decision artifact. The next code blocks, if approved, should be small and separate: `main`/`master` write cap, local emergency stop/daily cap, and only then a dependency graph for the runtime patch permit-consume tower.
