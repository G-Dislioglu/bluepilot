# BPK-017 Review Packet - Runtime Dry-Run Route Contract

## Summary

BPK-017 adds a pure runtime dry-run route contract.

- `builder/src/runtimeDryRunRouteContract.ts` validates a future HTTP request shape against a
  BPK-013 dry-run adapter plan.
- The contract requires POST, an explicit confirm phrase, matching instruction text, and a ready
  adapter plan.
- Blocked and review-required plans return deterministic non-2xx-style responses.
- No `server.ts`, route registration, orchestrator call, provider call, DB write, GitHub write,
  package change, deployment change, or frontend file was added.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-017.json` | WLP contract for dry-run route contract. |
| `builder/src/runtimeDryRunRouteContract.ts` | Pure route request/response contract. |
| `builder/tests/runtimeDryRunRouteContract.test.ts` | Focused route contract tests. |
| `docs/CLAUDE-CONTEXT.md` | Records BPK-017 and next-block posture. |
| `docs/SESSION-LOG.md` | Records BPK-017 result and test evidence. |
| `STATE.md` | Records current BPK contract and next sequence. |
| `review-packets/BPK-017.md` | This review packet. |

## Safety Invariants

| Invariant | Status | Evidence |
| --- | --- | --- |
| Contract is side-effect-free | PASS | It imports only the BPK-013 plan type and returns data. |
| No route registered | PASS | `server.ts` and `probeDryRun.ts` were not changed. |
| No runtime execution | PASS | No orchestrator or provider file was changed. |
| Confirmation required | PASS | Tests cover missing confirm. |
| Review plans are not success | PASS | Tests cover `409` review response. |

## Commands

```text
npx tsx --test tests/runtimeDryRunRouteContract.test.ts
npm run typecheck
node tools/verify-task-lock.cjs BPK-017 --verify
git diff --check
```

Observed before final verification:

| Check | Result |
| --- | --- |
| `npx tsx --test tests/runtimeDryRunRouteContract.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |

## Scope Notes

- This is not a mounted endpoint.
- Actual route wiring requires a later contract and should call this shape first.
- Runtime side effects remain closed.

## Next Block

Cockpit Read-Only UI may open only after BPK-017 verify, commit, and push are complete.
