TASK_ID: BP-126
TITLE: Builder TypeScript foundation subpackage
DATE: 2026-05-31

## Summary

BP-126 creates a separate TypeScript subpackage at `builder/` for the upcoming Builder migration.

No soulmatch Builder module was moved, copied, or imported.

## Claude Package Corrections

The supplied Claude package was useful, but not applied blindly.

Corrections:

- The supplied BP-126 contract lacked WLP-valid `reuse_target` and `evidence_required` fields.
- The supplied migration matrix had mojibake encoding and over-confident wording around the
  Tier-1 closure.
- `builder/package-lock.json` and `builder/.gitignore` were added to the allowed scope because
  a real npm install needs a lockfile and must not leave `node_modules` as untracked drift.
- `docs/SESSION-LOG.md` and `STATE.md` were included because Bluepilot's ANKER-REGEL requires
  sync after a block.

## Changed Files

- `contracts/BP-126.json`
- `builder/.gitignore`
- `builder/package.json`
- `builder/package-lock.json`
- `builder/tsconfig.json`
- `builder/src/index.ts`
- `builder/src/smoke.ts`
- `builder/tests/smoke.test.ts`
- `builder/README.md`
- `docs/BUILDER_MIGRATION_MATRIX.md`
- `docs/SESSION-LOG.md`
- `STATE.md`
- `review-packets/BP-126.md`

## Implementation Notes

- Bluepilot root stays CommonJS/governance-focused.
- `builder/` is a self-contained TypeScript ESM package.
- `tsconfig.json` uses `module: "NodeNext"` and `moduleResolution: "NodeNext"` to support
  `.js` import specifiers from TypeScript source.
- The smoke test imports `../src/smoke.js`, matching the import style expected by the future
  soulmatch Builder modules.
- `builder/node_modules/` is ignored by `builder/.gitignore`.

## Evidence

| Kind | Command | Result |
| --- | --- | --- |
| runtime_check | `node tools/verify-task-lock.cjs BP-126 --preflight` | PASS |
| runtime_check | `npm install` in `builder/` | PASS, 0 vulnerabilities |
| test_result | `npm test` in `builder/` | PASS, 1 test |
| runtime_check | `npm run typecheck` in `builder/` | PASS |
| runtime_check | `node tools/verify-task-lock.cjs BP-126 --verify` | PASS |
| runtime_check | `git diff --check` | PASS |
| runtime_check | `git diff -- package.json tools/verify-task-lock.cjs` | PASS, empty diff |
| runtime_check | search `builder/src`, `builder/tests` for soulmatch Builder imports | PASS, no matches |

## Guardrails

- Root `package.json` unchanged.
- `tools/*.cjs` unchanged.
- No database, provider, network, GitHub, or maya-core runtime path added.
- No soulmatch Builder module moved into Bluepilot.

## Result

Bluepilot now has a verified TypeScript home for the staged Builder migration.

Reuse durch: BP-127
