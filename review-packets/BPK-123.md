# BPK-123 Review Packet - Cockpit Patch Permit Consume Application Preflight

## Summary

BPK-123 adds a side-effect-free application preflight builder for cockpit patch permit consume authorization.

- `builder/src/cockpitPatchPermitConsumeApplicationPreflight.ts` exports `preflightCockpitPatchPermitConsumeApplication`.
- It requires application, operator, and application policy refs before application preflight can become ready.
- Permit consumption, patch application, server mutation, and route mutation remain false.

## Commands

| Check | Result |
| --- | --- |
| `npx tsx --test tests/cockpitPatchPermitConsumeApplicationPreflight.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `node tools/verify-task-lock.cjs BPK-123 --verify` | PASS - no drift |
| `git diff --check` | PASS |
| `npm test` in `builder/` | PASS - 185/185 tests |

## Scope Notes

- Consume application preflight only; no permit consumption, patch apply, server mount, route mutation, DB, package, workflow, deploy, durable write, or external action.
