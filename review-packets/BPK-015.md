# BPK-015 Review Packet - Live AICOS Fetch/Cache Contract

## Summary

BPK-015 adds a pure live AICOS fetch/cache readiness contract.

- `builder/src/liveAicosFetchCacheContract.ts` validates endpoint references, auth references,
  cache TTL, stale behavior, quarantine policy, and fetch limits.
- Token-like auth values are rejected; only opaque secret references are allowed.
- Cache-enabled fetch requires explicit TTL and stale behavior.
- No AICOS call, maya-core call, cache persistence, route, provider call, database write,
  GitHub write, package change, deployment change, runtime change, or frontend file was added.

## Changed Files

| File | Purpose |
| --- | --- |
| `contracts/BPK-015.json` | WLP contract for live AICOS fetch/cache readiness. |
| `builder/src/liveAicosFetchCacheContract.ts` | Pure fetch/cache readiness classifier. |
| `builder/tests/liveAicosFetchCacheContract.test.ts` | Focused classifier tests. |
| `docs/CLAUDE-CONTEXT.md` | Records BPK-015 and next-block posture. |
| `docs/SESSION-LOG.md` | Records BPK-015 result and test evidence. |
| `STATE.md` | Records current BPK contract and next sequence. |
| `review-packets/BPK-015.md` | This review packet. |

## Safety Invariants

| Invariant | Status | Evidence |
| --- | --- | --- |
| Contract is side-effect-free | PASS | It imports no runtime/provider modules and returns data. |
| No live fetch | PASS | No AICOS, maya-core, provider, route, or network code was added. |
| Secrets are not accepted inline | PASS | Tests cover token-like auth rejection. |
| Cache policy is explicit | PASS | Tests cover TTL and stale behavior. |
| Quarantine is mandatory | PASS | Tests cover missing quarantine block. |

## Commands

```text
npx tsx --test tests/liveAicosFetchCacheContract.test.ts
npm run typecheck
node tools/verify-task-lock.cjs BPK-015 --verify
git diff --check
```

Observed before final verification:

| Check | Result |
| --- | --- |
| `npx tsx --test tests/liveAicosFetchCacheContract.test.ts` | PASS |
| `npm run typecheck` in `builder/` | PASS |
| `npm test` in `builder/` | PASS, 67/67 tests |

## Scope Notes

- This block does not fetch cards.
- A later connector must pipe fetched snapshots through BPK-010 intake.
- Auth references are names only; no token value belongs in the contract.

## Next Block

The requested block set is complete after BPK-015 verify, commit, and push.
